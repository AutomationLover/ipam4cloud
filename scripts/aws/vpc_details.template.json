{
  "aws_account_id": "${AWS_ACCOUNT_ID}",
  "region": "${AWS_DEFAULT_REGION}",
  "created_date": "2025-09-19",
  "created_by": "your-role/your-email@company.com",
  "vpcs": [
    {
      "name": "${TEST_VPC_1_NAME}",
      "vpc_id": "${TEST_VPC_1_ID}",
      "cidr_block": "${TEST_VPC_1_CIDR}",
      "status": "created",
      "purpose": "ipam4cloud testing - primary VPC"
    },
    {
      "name": "${TEST_VPC_2_NAME}",
      "vpc_id": "${TEST_VPC_2_ID}",
      "cidr_block": "${TEST_VPC_2_CIDR}",
      "status": "created",
      "purpose": "ipam4cloud testing - secondary VPC"
    }
  ],
  "existing_vpcs_in_region": [
    {
      "vpc_id": "vpc-xxxxxxxxx",
      "cidr_block": "172.31.0.0/16",
      "name": "default-vpc"
    }
  ],
  "notes": [
    "Successfully created both VPCs in ${AWS_DEFAULT_REGION} region",
    "VPC 1: ${TEST_VPC_1_ID} with CIDR ${TEST_VPC_1_CIDR}",
    "VPC 2: ${TEST_VPC_2_ID} with CIDR ${TEST_VPC_2_CIDR}",
    "These VPCs can be used as real AWS examples for the ipam4cloud app to fetch subnet CIDRs"
  ]
}
